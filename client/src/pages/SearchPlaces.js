import React from 'react';
import { useQuery, useMutation } from '@apollo/client';
import {  Container,  Card,  Button,  Row,  Col} from 'react-bootstrap';

const SearchPlaces = () => {
  return (
    <h1>SearchPlaces_Testing</h1>
  );
};

export default SearchPlaces;